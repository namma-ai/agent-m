/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.{js,jsx,ts,tsx}'], // Include all TypeScript/JavaScript files
    theme: {
      extend: {}, // Add customizations here if needed
    },
    plugins: [], // Add plugins like forms or typography if required
  };